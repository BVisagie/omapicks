{
  "name": "omapicks",
  "version": "0.1.0",
  "private": true,
  "description": "Independent weekly rankings for Omarchy plugins",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node build/index.mjs",
    "refresh": "node build/refresh.mjs",
    "test": "node --test",
    "check": "node --test && node build/index.mjs"
  }
}
